import { GuardadasClient } from "@/components/chazas/guardadas-client"

export const metadata = {
  title: "Guardadas | ChazasUN",
}

export default function GuardadasPage() {
  return <GuardadasClient />
}
